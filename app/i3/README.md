# I3

```bash
sudo apt install ansible i3
git clone https://github.com/justmeandopensource/myi3 $HOME/Scaricati/myi3
cd $HOME/Scaricati/myi3
ansible-playbook -i ansible.cfg setup.yaml                 (edit the username in setup.yaml before running)
```